import React from 'react';

const Navbar = () => {
    return (
        <div>
            <input type="text" name="search" placeholder="Departement ou région" />
        </div>
    );
};

export default Navbar;